{{- if .Values.msgTopologyOperator.networkPolicy.enabled }}
kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: {{ template "rmqco.msgTopologyOperator.fullname" . }}
  namespace: {{ include "cloudpirates.namespace" . }}
  labels:
    {{- include "rmqmto.labels" . | nindent 4}}
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "cloudpirates.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  {{- $podLabels := include "cloudpirates.tplvalues.merge" ( dict "values" ( list .Values.msgTopologyOperator.podLabels .Values.commonLabels ) "context" . ) }}
  podSelector:
    matchLabels:
      {{- include "rmqco.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: messaging-topology-operator
      app.kubernetes.io/part-of: rabbitmq
  policyTypes:
    - Ingress
    - Egress
  {{- if .Values.msgTopologyOperator.networkPolicy.allowExternalEgress }}
  egress:
    - {}
  {{- else }}
  egress:
    - ports:
        # Allow dns resolution
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
        # Allow rabbitmq api
        - port: 15672
          protocol: TCP
        # Allow access to kube-apiserver
        {{- range $port := .Values.msgTopologyOperator.networkPolicy.kubeAPIServerPorts }}
        - port: {{ $port }}
        {{- end }}
    # RabbitMQCluster instances have the label app.kubernetes.io/component: rabbitmq
    - to:
        - podSelector:
            matchLabels:
              app.kubernetes.io/component: rabbitmq
        {{- if not .Values.msgTopologyOperator.watchAllNamespaces }}
        {{- $watchNamespaces := default (list (include "cloudpirates.namespace" .)) .Values.msgTopologyOperator.watchNamespaces }}
          namespaceSelector:
            matchExpressions:
              - key: namespace
                operator: In
                values:
                {{- range $namespace := $watchNamespaces }}
                  - {{ $namespace }}
                {{- end }}
        {{- end }}
    {{- if .Values.msgTopologyOperator.networkPolicy.extraEgress }}
    {{- include "cloudpirates.tplvalues.render" ( dict "value" .Values.msgTopologyOperator.networkPolicy.extraEgress "context" $ ) | nindent 4 }}
    {{- end }}
  {{- end }}
  ingress:
    - ports:
        {{/* Webhook port is hardcoded in the operator code */}}
        - port: 9443
        - port: {{ .Values.msgTopologyOperator.containerPorts.health }}
        {{- if .Values.msgTopologyOperator.metrics.service.enabled }}
        - port: {{ .Values.msgTopologyOperator.containerPorts.metrics }}
        {{- end }}
      {{- if not .Values.msgTopologyOperator.networkPolicy.allowExternal }}
      from:
        - podSelector:
            matchLabels:
              {{- include "rmqco.selectorLabels" . | nindent 14 }}
              app.kubernetes.io/part-of: rabbitmq
        - podSelector:
            matchLabels:
              {{ template "rmqco.msgTopologyOperator.fullname" . }}-client: "true"
        {{- if .Values.msgTopologyOperator.networkPolicy.ingressNSMatchLabels }}
        - namespaceSelector:
            matchLabels:
              {{- range $key, $value := .Values.msgTopologyOperator.networkPolicy.ingressNSMatchLabels }}
              {{ $key | quote }}: {{ $value | quote }}
              {{- end }}
          {{- if .Values.msgTopologyOperator.networkPolicy.ingressNSPodMatchLabels }}
          podSelector:
            matchLabels:
              {{- range $key, $value := .Values.msgTopologyOperator.networkPolicy.ingressNSPodMatchLabels }}
              {{ $key | quote }}: {{ $value | quote }}
              {{- end }}
          {{- end }}
        {{- end }}
      {{- end }}
    {{- if .Values.msgTopologyOperator.networkPolicy.extraIngress }}
    {{- include "cloudpirates.tplvalues.render" ( dict "value" .Values.msgTopologyOperator.networkPolicy.extraIngress "context" $ ) | nindent 4 }}
    {{- end }}
{{- end }}
