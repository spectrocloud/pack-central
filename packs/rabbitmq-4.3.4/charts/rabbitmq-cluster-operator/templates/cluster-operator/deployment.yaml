apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "rmqco.clusterOperator.fullname" . }}
  namespace: {{ include "cloudpirates.namespace" . }}
  labels:
    {{- include "rmqco.labels" . | nindent 4 }}
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "cloudpirates.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ .Values.clusterOperator.replicaCount }}
  revisionHistoryLimit: {{ .Values.clusterOperator.revisionHistoryLimit }}
  {{- if .Values.clusterOperator.updateStrategy }}
  strategy: {{- toYaml .Values.clusterOperator.updateStrategy | nindent 4 }}
  {{- end }}
  {{- $podLabels := include "cloudpirates.tplvalues.merge" ( dict "values" ( list .Values.clusterOperator.podLabels .Values.commonLabels ) "context" . ) }}
  selector:
    matchLabels:
      {{- include "rmqco.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: rabbitmq-operator
  template:
    metadata:
      {{- if .Values.clusterOperator.podAnnotations }}
      annotations: {{- include "cloudpirates.tplvalues.render" (dict "value" .Values.clusterOperator.podAnnotations "context" $) | nindent 8 }}
      {{- end }}
      labels:
        {{- include "rmqco.labels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ template "rmqco.clusterOperator.serviceAccountName" . }}
      {{- include "rmqco.imagePullSecrets" . | indent 6 }}
      {{- if .Values.clusterOperator.schedulerName }}
      schedulerName: {{ .Values.clusterOperator.schedulerName | quote }}
      {{- end }}
      automountServiceAccountToken: {{ .Values.clusterOperator.automountServiceAccountToken }}
      {{- if .Values.clusterOperator.hostAliases }}
      hostAliases: {{- include "cloudpirates.tplvalues.render" (dict "value" .Values.clusterOperator.hostAliases "context" $) | nindent 8 }}
      {{- end }}
      {{- if .Values.clusterOperator.topologySpreadConstraints }}
      topologySpreadConstraints: {{- include "cloudpirates.tplvalues.render" (dict "value" .Values.clusterOperator.topologySpreadConstraints "context" $) | nindent 8 }}
      {{- end }}
      {{- if .Values.clusterOperator.affinity }}
      affinity: {{- include "cloudpirates.tplvalues.render" ( dict "value" .Values.clusterOperator.affinity "context" $) | nindent 8 }}
      {{- end }}
      {{- if .Values.clusterOperator.nodeSelector }}
      nodeSelector: {{- include "cloudpirates.tplvalues.render" ( dict "value" .Values.clusterOperator.nodeSelector "context" $) | nindent 8 }}
      {{- end }}
      {{- if .Values.clusterOperator.tolerations }}
      tolerations: {{- include "cloudpirates.tplvalues.render" (dict "value" .Values.clusterOperator.tolerations "context" .) | nindent 8 }}
      {{- end }}
      {{- if .Values.clusterOperator.priorityClassName }}
      priorityClassName: {{ .Values.clusterOperator.priorityClassName | quote }}
      {{- end }}
      {{- if .Values.clusterOperator.podSecurityContext.enabled }}
      securityContext:
        {{- include "rmqco.renderPodSecurityContext" (dict "securityContext" .Values.clusterOperator.podSecurityContext "context" .) | nindent 8 }}
      {{- end }}
      {{- if .Values.clusterOperator.terminationGracePeriodSeconds }}
      terminationGracePeriodSeconds: {{ .Values.clusterOperator.terminationGracePeriodSeconds }}
      {{- end }}
      initContainers:
        {{- if .Values.clusterOperator.initContainers }}
        {{- include "cloudpirates.tplvalues.render" (dict "value" .Values.clusterOperator.initContainers "context" $) | nindent 8 }}
        {{- end }}
      containers:
        - name: rabbitmq-cluster-operator
          image: {{ template "rmqco.clusterOperator.image" . }}
          imagePullPolicy: {{ .Values.clusterOperator.image.pullPolicy }}
          {{- if .Values.clusterOperator.containerSecurityContext.enabled }}
          securityContext:
            {{- include "rmqco.renderContainerSecurityContext" (dict "securityContext" .Values.clusterOperator.containerSecurityContext "context" .) | nindent 12 }}
          {{- end }}
          {{- if .Values.clusterOperator.command }}
          command: {{- include "cloudpirates.tplvalues.render" (dict "value" .Values.clusterOperator.command "context" $) | nindent 12 }}
          {{- else }}
          command:
            - /manager
          {{- end }}
          {{- if .Values.clusterOperator.args }}
          args: {{- include "cloudpirates.tplvalues.render" (dict "value" .Values.clusterOperator.args "context" $) | nindent 12 }}
          {{- else }}
          args:
            - --metrics-bind-address=:{{ .Values.clusterOperator.containerPorts.metrics }}
            - --health-probe-bind-address=:{{ .Values.clusterOperator.containerPorts.health }}
            {{- if .Values.clusterOperator.logLevel }}
            - --zap-log-level={{ .Values.clusterOperator.logLevel }}
            {{- end }}
            {{- if .Values.clusterOperator.extraArgs }}
            {{- include "cloudpirates.tplvalues.render" (dict "value" .Values.clusterOperator.extraArgs "context" $) | nindent 12 }}
            {{- end }}
          {{- end }}
          env:
            - name: OPERATOR_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            {{- if not .Values.clusterOperator.watchAllNamespaces }}
            {{- $watchNamespaces := default (list (include "cloudpirates.namespace" .)) .Values.clusterOperator.watchNamespaces }}
            - name: OPERATOR_SCOPE_NAMESPACE
              value: {{ join "," $watchNamespaces | quote }}
            {{- end }}
            {{- if not .Values.clusterOperator.webhook.enabled }}
            - name: ENABLE_WEBHOOKS
              value: "false"
            {{- end }}
            - name: DEFAULT_RABBITMQ_IMAGE
              value: {{ include "rmqco.rabbitmq.image" . }}
            - name: DEFAULT_USER_UPDATER_IMAGE
              value: {{ include "rmqco.defaultCredentialUpdater.image" . }}
            {{- if (include "rmqco.imagePullSecrets.string" .) }}
            - name: DEFAULT_IMAGE_PULL_SECRETS
              value: {{ include "rmqco.imagePullSecrets.string" . | quote }}
            {{- end }}
            {{- if .Values.clusterOperator.extraEnvVars }}
            {{- include "cloudpirates.tplvalues.render" (dict "value" .Values.clusterOperator.extraEnvVars "context" $) | nindent 12 }}
            {{- end }}
          envFrom:
            {{- if .Values.clusterOperator.extraEnvVarsCM }}
            - configMapRef:
                name: {{ include "cloudpirates.tplvalues.render" (dict "value" .Values.clusterOperator.extraEnvVarsCM "context" $) }}
            {{- end }}
            {{- if .Values.clusterOperator.extraEnvVarsSecret }}
            - secretRef:
                name: {{ include "cloudpirates.tplvalues.render" (dict "value" .Values.clusterOperator.extraEnvVarsSecret "context" $) }}
            {{- end }}
          {{- if .Values.clusterOperator.resources }}
          resources: {{- toYaml .Values.clusterOperator.resources | nindent 12 }}
          {{- end }}
          {{- if not .Values.diagnosticMode.enabled }}
          {{- if .Values.clusterOperator.customLivenessProbe }}
          livenessProbe: {{- include "cloudpirates.tplvalues.render" (dict "value" .Values.clusterOperator.customLivenessProbe "context" $) | nindent 12 }}
          {{- else if .Values.clusterOperator.livenessProbe.enabled }}
          livenessProbe: {{- include "cloudpirates.tplvalues.render" (dict "value" (omit .Values.clusterOperator.livenessProbe "enabled") "context" $) | nindent 12 }}
            httpGet:
              path: /healthz
              port: health
          {{- end }}
          {{- if .Values.clusterOperator.customReadinessProbe }}
          readinessProbe: {{- include "cloudpirates.tplvalues.render" (dict "value" .Values.clusterOperator.customReadinessProbe "context" $) | nindent 12 }}
          {{- else if .Values.clusterOperator.readinessProbe.enabled }}
          readinessProbe: {{- include "cloudpirates.tplvalues.render" (dict "value" (omit .Values.clusterOperator.readinessProbe "enabled") "context" $) | nindent 12 }}
            httpGet:
              path: /readyz
              port: health
          {{- end }}
          {{- if .Values.clusterOperator.customStartupProbe }}
          startupProbe: {{- include "cloudpirates.tplvalues.render" (dict "value" .Values.clusterOperator.customStartupProbe "context" $) | nindent 12 }}
          {{- else if .Values.clusterOperator.startupProbe.enabled }}
          startupProbe: {{- include "cloudpirates.tplvalues.render" (dict "value" (omit .Values.clusterOperator.startupProbe "enabled") "context" $) | nindent 12 }}
            httpGet:
              path: /readyz
              port: health
          {{- end }}
          {{- end }}
          {{- if .Values.clusterOperator.lifecycleHooks }}
          lifecycle: {{- include "cloudpirates.tplvalues.render" (dict "value" .Values.clusterOperator.lifecycleHooks "context" $) | nindent 12 }}
          {{- end }}
          {{- if or .Values.clusterOperator.webhook.enabled .Values.clusterOperator.extraVolumeMounts }}
          volumeMounts:
            {{- if .Values.clusterOperator.webhook.enabled }}
            - mountPath: /tmp/k8s-webhook-server/serving-certs
              name: cert
              readOnly: true
            {{- end }}
            {{- if .Values.clusterOperator.extraVolumeMounts }}
            {{- include "cloudpirates.tplvalues.render" (dict "value" .Values.clusterOperator.extraVolumeMounts "context" $) | nindent 12 }}
            {{- end }}
          {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.clusterOperator.containerPorts.metrics }}
              protocol: TCP
            - name: health
              containerPort: {{ .Values.clusterOperator.containerPorts.health }}
              protocol: TCP
            {{- if .Values.clusterOperator.webhook.enabled }}
            - name: webhook-server
              containerPort: 9443
              protocol: TCP
            {{- end }}
        {{- if .Values.clusterOperator.sidecars }}
        {{- include "cloudpirates.tplvalues.render" ( dict "value" .Values.clusterOperator.sidecars "context" $) | nindent 8 }}
        {{- end }}
      {{- if or .Values.clusterOperator.webhook.enabled .Values.clusterOperator.extraVolumes }}
      volumes:
        {{- if .Values.clusterOperator.webhook.enabled }}
        - name: cert
          secret:
            defaultMode: 420
            secretName: {{ template "rmqco.clusterOperator.webhook.secretName" . }}
        {{- end }}
        {{- if .Values.clusterOperator.extraVolumes }}
        {{- include "cloudpirates.tplvalues.render" (dict "value" .Values.clusterOperator.extraVolumes "context" $) | nindent 8 }}
        {{- end }}
      {{- end }}
