# Copyright The Kubernetes Authors
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#    https://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

{{- if .Values.resources.computeDomains.enabled }}
{{- include "dra-driver-nvidia-gpu.validateIMEXConfig" . }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "dra-driver-nvidia-gpu.name" . }}-controller
  namespace: {{ include "dra-driver-nvidia-gpu.namespace" . }}
  labels:
    {{- include "dra-driver-nvidia-gpu.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.controller.replicas }}
  selector:
    matchLabels:
      {{- include "dra-driver-nvidia-gpu.selectorLabels" (dict "context" . "componentName" "controller") | nindent 6 }}
  template:
    metadata:
      {{- with .Values.controller.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "dra-driver-nvidia-gpu.templateLabels" . | nindent 8 }}
        {{- include "dra-driver-nvidia-gpu.selectorLabels" (dict "context" . "componentName" "controller") | nindent 8 }}
    spec:
      {{- if .Values.controller.priorityClassName }}
      priorityClassName: {{ .Values.controller.priorityClassName }}
      {{- end }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "dra-driver-nvidia-gpu.serviceAccountName" . }}-controller
      securityContext:
        {{- toYaml .Values.controller.podSecurityContext | nindent 8 }}
      containers:
      - name: compute-domain
        securityContext:
          {{- toYaml .Values.controller.containers.computeDomain.securityContext | nindent 10 }}
        image: {{ include "dra-driver-nvidia-gpu.fullimage" . }}
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        command: ["compute-domain-controller", "-v", "$(LOG_VERBOSITY)"]
        resources:
          {{- toYaml .Values.controller.containers.computeDomain.resources | nindent 10 }}
        env:
        {{- if .Values.controller.metrics.enabled }}
        # HTTP_ENDPOINT and METRICS_PATH are used to configure the metrics HTTP server.
        - name: HTTP_ENDPOINT
          value: "{{ .Values.controller.metrics.httpEndpoint }}"
        - name: METRICS_PATH
          value: "{{ .Values.controller.metrics.metricsPath }}"
        - name: PPROF_PATH
          value: "{{ .Values.controller.metrics.profilePath }}"
        {{- end }}
        # LOG_VERBOSITY is the source of truth for this program's klog
        # configuration. Currently injected via CLI argument (see above) because
        # klog's verbosity for now cannot be sanely set from an env var.
        - name: LOG_VERBOSITY
          value: "{{ .Values.logVerbosity }}"
        # LOG_VERBOSITY_CD_DAEMON controls the verbosity of dynamically launched
        # CD daemons (their pod spec is not rendered by Helm, but by this
        # controller).
        - name: LOG_VERBOSITY_CD_DAEMON
          value: "{{ .Values.logVerbosity }}"
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: IMAGE_NAME
          value: {{ include "dra-driver-nvidia-gpu.fullimage" . }}
        {{- if .Values.imagePullSecrets }}
        # Comma-separated secret names (from chart imagePullSecrets) for
        # compute-domain-daemon DaemonSet pod specs.
        - name: CD_DAEMON_IMAGE_PULL_SECRET_NAMES
          value: "{{- range $i, $s := .Values.imagePullSecrets }}{{ if $i }},{{ end }}{{ $s.name }}{{ end }}"
        {{- end }}
        # Use runc: explicit "void"; otherwise we inherit "all".
        - name: NVIDIA_VISIBLE_DEVICES
          value: void
        {{- if .Values.featureGates }}
        # Feature gates (includes both project-specific and logging features)
        - name: FEATURE_GATES
          value: "{{ range $key, $value := .Values.featureGates }}{{ $key }}={{ $value }},{{ end }}"
        {{- end }}
        - name: IMEX_MODE
          value: "{{ .Values.resources.computeDomains.imex.mode }}"
        - name: IMEX_ISOLATION
          value: "{{ .Values.resources.computeDomains.imex.isolation }}"
        {{- with .Values.controller.containers.computeDomain.env }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        - name: LEADER_ELECTION_ENABLED
          value: "{{ .Values.controller.leaderElection.enabled }}"
        - name: LEADER_ELECTION_LEASE_LOCK_NAME
          value: "{{ include "dra-driver-nvidia-gpu.name" . }}-controller"
        - name: LEADER_ELECTION_LEASE_LOCK_NAMESPACE
          value: "{{ include "dra-driver-nvidia-gpu.namespace" . }}"
        - name: LEADER_ELECTION_LEASE_DURATION
          value: "{{ .Values.controller.leaderElection.leaseDuration }}"
        - name: LEADER_ELECTION_RENEW_DEADLINE
          value: "{{ .Values.controller.leaderElection.renewDeadline }}"
        - name: LEADER_ELECTION_RETRY_PERIOD
          value: "{{ .Values.controller.leaderElection.retryPeriod }}"
      {{- with .Values.controller.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.controller.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.controller.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
