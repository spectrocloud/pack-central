{{- if and .Values.webhook.enabled (eq .Values.webhook.tls.mode "cert-manager") }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "dra-driver-nvidia-gpu.name" . }}-webhook-cert
  namespace: {{ include "dra-driver-nvidia-gpu.namespace" . }}
  labels:
    {{- include "dra-driver-nvidia-gpu.labels" . | nindent 4 }}
spec:
  dnsNames:
    {{- $svcName := printf "%s-webhook" (include "dra-driver-nvidia-gpu.name" .) }}
    {{- $svcNamespace := (include "dra-driver-nvidia-gpu.namespace" .) }}
    - {{ $svcName }}.{{ $svcNamespace }}.svc
    {{- with .Values.webhook.tls.certManager.dnsNames }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  issuerRef:
    {{- if eq .Values.webhook.tls.certManager.issuerType "clusterissuer" }}
    kind: ClusterIssuer
    {{- else }}
    kind: Issuer
    {{- end }}
    {{- if eq .Values.webhook.tls.certManager.issuerType "selfsigned" }}
    name: {{ include "dra-driver-nvidia-gpu.fullname" . }}-webhook-issuer
    {{- else }}
    name: {{ .Values.webhook.tls.certManager.issuerName }}
    {{- end }}
  secretName: {{ include "dra-driver-nvidia-gpu.name" . }}-webhook-cert
{{- end }}
