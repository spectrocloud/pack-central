{{- if .Values.webhook.enabled }}
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingWebhookConfiguration
metadata:
  name: {{ include "dra-driver-nvidia-gpu.name" . }}-webhook-config
  labels:
    {{- include "dra-driver-nvidia-gpu.labels" . | nindent 4 }}
  {{- if eq .Values.webhook.tls.mode "cert-manager" }}
  annotations:
    cert-manager.io/inject-ca-from: "{{ include "dra-driver-nvidia-gpu.namespace" . }}/{{ include "dra-driver-nvidia-gpu.name" . }}-webhook-cert"
  {{- end }}
webhooks:
  - name: "gpu.nvidia.com"
    rules:
    - apiGroups:   ["resource.k8s.io"]
      apiVersions: ["v1beta1", "v1beta2", "v1"]
      operations:  ["CREATE", "UPDATE"]
      resources:   ["resourceclaims", "resourceclaimtemplates"]
      scope:       "Namespaced"
    clientConfig:
      service:
        namespace: {{ include "dra-driver-nvidia-gpu.namespace" . }}
        name: {{ include "dra-driver-nvidia-gpu.name" . }}-webhook
        port: {{ .Values.webhook.servicePort }}
        path: /validate-resource-claim-parameters
      {{- if and (eq .Values.webhook.tls.mode "secret") .Values.webhook.tls.secret.caBundle }}
      caBundle: {{ .Values.webhook.tls.secret.caBundle | quote }}
      {{- end }}
    failurePolicy: {{ default "Fail" .Values.webhook.failurePolicy | quote }}
    admissionReviewVersions: ["v1"]
    sideEffects: None
{{- end }}
