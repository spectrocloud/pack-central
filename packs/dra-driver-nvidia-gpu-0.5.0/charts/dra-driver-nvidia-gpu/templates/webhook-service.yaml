{{- if .Values.webhook.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "dra-driver-nvidia-gpu.name" . }}-webhook
  namespace: {{ include "dra-driver-nvidia-gpu.namespace" . }}
  labels:
    {{- include "dra-driver-nvidia-gpu.labels" . | nindent 4 }}
spec:
  ports:
  - port: {{ .Values.webhook.servicePort }}
    targetPort: webhook
    protocol: TCP
    name: https
  selector:
    {{- include "dra-driver-nvidia-gpu.selectorLabels" (dict "context" . "componentName" "webhook") | nindent 4 }}
{{- end }}
