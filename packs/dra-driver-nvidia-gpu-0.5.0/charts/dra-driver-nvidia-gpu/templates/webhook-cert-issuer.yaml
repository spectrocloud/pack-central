{{- if and .Values.webhook.enabled (eq .Values.webhook.tls.mode "cert-manager") (eq .Values.webhook.tls.certManager.issuerType "selfsigned") }}
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ include "dra-driver-nvidia-gpu.fullname" . }}-webhook-issuer
  namespace: {{ include "dra-driver-nvidia-gpu.namespace" . }}
  labels:
    {{- include "dra-driver-nvidia-gpu.labels" . | nindent 4 }}
spec:
  selfSigned: {}
{{- end }}
