apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
    helm.sh/resource-policy: keep
  labels:
    app.kubernetes.io/part-of: dpf-operator-controller-manager
    dpu.nvidia.com/component: dpf-operator-controller-manager
  name: dpuvirtualnetworks.vpc.dpu.nvidia.com
spec:
  group: vpc.dpu.nvidia.com
  names:
    kind: DPUVirtualNetwork
    listKind: DPUVirtualNetworkList
    plural: dpuvirtualnetworks
    singular: dpuvirtualnetwork
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DPUVirtualNetwork is the Schema for the dpuvirtualnetwork API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DPUVirtualNetworkSpec defines the desired state of DPUVirtualNetworkSpec
            properties:
              bridgedNetwork:
                description: BridgedNetwork contains the bridged network configuration
                properties:
                  ipam:
                    description: IPAM contains the IPAM configuration for the bridged
                      network
                    properties:
                      ipv4:
                        description: IPv4 contains the IPv4 IPAM configuration
                        properties:
                          dhcp:
                            description: DHCP if set, enables DHCP for the network
                            type: boolean
                          excludeIPs:
                            description: ExcludeIPs are the IPs to exclude from DHCP
                              allocation.
                            items:
                              properties:
                                ip:
                                  description: IP is the IP address to exclude from
                                    DHCP allocation. must be part for the virtual
                                    network subnet.
                                  type: string
                                range:
                                  description: Range is the range of IP addresses
                                    to exclude from DHCP allocation. must be part
                                    for the virtual network subnet.
                                  properties:
                                    end:
                                      description: End is the end of the IP address
                                        range
                                      type: string
                                    start:
                                      description: Start is the start of the IP address
                                        range
                                      type: string
                                  required:
                                  - end
                                  - start
                                  type: object
                              type: object
                            type: array
                          subnet:
                            description: Subnet is the network subnet in CIDR format
                              to use for DHCP. the first IP in the subnet is the gateway.
                            type: string
                        required:
                        - dhcp
                        - subnet
                        type: object
                    type: object
                type: object
              externallyRouted:
                description: ExternallyRouted defines if the virtual network can be
                  routed externally
                type: boolean
              masquerade:
                default: true
                description: |-
                  Masquerade defines if the virtual network should masquerade the traffic before egressing to external networks.
                  valid only if ExternallyRouted is true
                type: boolean
              nodeSelector:
                description: NodeSelector Selects the DPU Nodes with specific labels
                  which can belong to the virtual network.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              type:
                description: Type of the virtual network
                enum:
                - Bridged
                type: string
              vpcName:
                description: vpcName is the name of the DPUVPC the virtual network
                  belongs within the same namespace.
                type: string
            required:
            - externallyRouted
            - type
            - vpcName
            type: object
            x-kubernetes-validations:
            - message: DPUVirtualNetwork spec is immutable
              rule: self == oldSelf
            - message: for type=Bridged, bridgedNetwork must be set
              rule: (self.type == 'Bridged' && has(self.bridgedNetwork))
          status:
            description: DPUVirtualNetworkStatus defines the observed state of DPUVirtualNetwork
            properties:
              conditions:
                description: Conditions reflect the status of the object
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration records the Generation observed on
                  the object the last time it was patched.
                format: int64
                type: integer
            type: object
        type: object
    selectableFields:
    - jsonPath: .spec.vpcName
    served: true
    storage: true
    subresources:
      status: {}
