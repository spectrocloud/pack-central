# Generated by protoc-gen-crd from external_remediation.proto

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: externalremediationrequests.nvsentinel.dgxc.nvidia.com
  annotations: {}
spec:
  group: nvsentinel.dgxc.nvidia.com
  scope: Cluster
  names:
    kind: ExternalRemediationRequest
    listKind: ExternalRemediationRequestList
    plural: externalremediationrequests
    singular: externalremediationrequest
    shortNames:
      - extrr
    categories:
      - nvsentinel
  versions:
    - name: v1
      served: true
      storage: true
      subresources:
        status: {}
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              nullable: true
              type: object
              properties:
                healthEvent:
                  nullable: true
                  type: object
                  properties:
                    version:
                      type: integer
                      format: uint32
                    agent:
                      type: string
                    componentClass:
                      type: string
                    checkName:
                      type: string
                    isFatal:
                      type: boolean
                    isHealthy:
                      type: boolean
                    message:
                      type: string
                    recommendedAction:
                      enum:
                        - 0
                        - "NONE"
                        - 2
                        - "COMPONENT_RESET"
                        - 5
                        - "CONTACT_SUPPORT"
                        - 6
                        - "RUN_FIELDDIAG"
                        - 15
                        - "RESTART_VM"
                        - 24
                        - "RESTART_BM"
                        - 25
                        - "REPLACE_VM"
                        - 26
                        - "RUN_DCGMEUD"
                        - 27
                        - "CUSTOM"
                        - 99
                        - "UNKNOWN"
                      format: enum
                      x-kubernetes-int-or-string: true
                    errorCode:
                      type: array
                      items:
                        type: string
                    entitiesImpacted:
                      type: array
                      items:
                        nullable: true
                        type: object
                        properties:
                          entityType:
                            type: string
                          entityValue:
                            type: string
                    metadata:
                      type: object
                      additionalProperties:
                        type: string
                    generatedTimestamp:
                      type: string
                      format: RFC3339
                    nodeName:
                      type: string
                    quarantineOverrides:
                      nullable: true
                      type: object
                      properties:
                        force:
                          type: boolean
                        skip:
                          type: boolean
                    drainOverrides:
                      nullable: true
                      type: object
                      properties:
                        force:
                          type: boolean
                        skip:
                          type: boolean
                    processingStrategy:
                      enum:
                        - 0
                        - "UNSPECIFIED"
                        - 1
                        - "EXECUTE_REMEDIATION"
                        - 2
                        - "STORE_ONLY"
                        - 3
                        - "STORE_AND_ANALYSE"
                      format: enum
                      x-kubernetes-int-or-string: true
                    id:
                      type: string
                    customRecommendedAction:
                      type: string
                  description: healthEvent is a full copy of the triaged HealthEvent that produced this ExtRR. Reuses the existing HealthEvent message by reference; adding a field to HealthEvent propagates here automatically.
            status:
              nullable: true
              type: object
              properties:
                conditions:
                  type: array
                  items:
                    nullable: true
                    type: object
                    properties:
                      type:
                        type: string
                        description: type is the condition type (e.g. "NVSentinelOwnershipReleased", "ExternalRemediationComplete").
                      status:
                        type: string
                        description: status is one of "True", "False", "Unknown".
                      observedGeneration:
                        description: observedGeneration is the .metadata.generation that the condition was set based upon.
                        format: int64
                        x-kubernetes-int-or-string: true
                      lastTransitionTime:
                        type: string
                        description: lastTransitionTime is the last time the condition transitioned from one status to another.
                        format: RFC3339
                      reason:
                        type: string
                        description: reason contains a programmatic identifier indicating the reason for the condition's last transition (e.g. "ReleaseTaintApplied", "AwaitingExternalSystem", "ReleaseTaintFailed").
                      message:
                        type: string
                        description: message is a human-readable message indicating details about the transition.
                    description: Condition is the wire equivalent of metav1.Condition. The janitor/pkg/condition adapter package converts between this proto representation and metav1.Condition at the controller boundary so existing controller-runtime helpers (meta.SetStatusCondition, meta.IsStatusConditionTrue) continue to work.
                  description: conditions represent the latest available observations of the ExtRR's current state. See ADR-040 for the canonical type names and reasons.
                completionTime:
                  type: string
                  description: completionTime is set when the reconciler is done with this ExtRR — the apply terminated (success or NodeNotFound), the close path ran after Complete=True, or operator-initiated cleanup completed. Once set, the reconciler short-circuits to a CheckUnlock-only pass on subsequent reconciles. The TTL reconciler counts the cleanup window from this timestamp, matching the sibling janitor CRDs (RebootNode, TerminateNode, GPUReset).
                  format: RFC3339
          description: ExternalRemediationRequest is the exit door from NVSentinel ownership. Created by fault-remediation when triage maps a fault to the CUSTOM recommended action (per ADR-036) with a customRecommendedAction configured to produce an ExtRR. See ADR-040 (docs/designs/040-external-remediation-request.md) for the full design including the asymmetric ExternalRemediationComplete=True vs False handling, the cleanup finalizer, the release taint, and the nvsentinel.dgxc.nvidia.com/managed label coordination mechanism.
      additionalPrinterColumns:
        - name: Node
          type: string
          jsonPath: .spec.healthEvent.nodeName
        - name: CheckName
          type: string
          jsonPath: .spec.healthEvent.checkName
        - name: CompletionTime
          type: string
          format: date
          jsonPath: .status.completionTime
