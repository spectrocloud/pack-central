apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
    helm.sh/resource-policy: keep
  labels:
    app.kubernetes.io/part-of: dpf-operator-controller-manager
    dpu.nvidia.com/component: dpf-operator-controller-manager
  name: isolationclasses.vpc.dpu.nvidia.com
spec:
  group: vpc.dpu.nvidia.com
  names:
    kind: IsolationClass
    listKind: IsolationClassList
    plural: isolationclasses
    singular: isolationclass
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: IsolationClass is the Schema for the isolationclass API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: IsolationClassSpec defines the configuration of IsolationClass
            properties:
              parameters:
                additionalProperties:
                  type: string
                description: Parameters holds the parameters for the provisioner
                type: object
              provisioner:
                description: Provisioner indicates the type of the provisioner.
                type: string
            required:
            - provisioner
            type: object
            x-kubernetes-validations:
            - message: IsolationClass spec is immutable
              rule: self == oldSelf
          status:
            description: IsolationClassStatus defines the status of IsolationClass
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
