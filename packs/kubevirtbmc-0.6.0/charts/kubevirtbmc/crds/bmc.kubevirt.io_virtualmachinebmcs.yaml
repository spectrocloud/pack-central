---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: virtualmachinebmcs.bmc.kubevirt.io
spec:
  group: bmc.kubevirt.io
  names:
    kind: VirtualMachineBMC
    listKind: VirtualMachineBMCList
    plural: virtualmachinebmcs
    shortNames:
    - vmbmc
    - vmbmcs
    singular: virtualmachinebmc
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.virtualMachineRef.name
      name: VIRTUALMACHINE
      type: string
    - jsonPath: .spec.authSecretRef.name
      name: SECRET
      type: string
    - jsonPath: .status.clusterIP
      name: CLUSTERIP
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: VirtualMachineBMC is the Schema for the virtualmachinebmcs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VirtualMachineBMCSpec defines the desired state of VirtualMachineBMC.
            properties:
              authSecretRef:
                description: Reference to the Secret containing IPMI/Redfish credentials.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              ipmi:
                description: IPMI configures the IPMI simulator.
                properties:
                  enabled:
                    description: Enabled toggles the IPMI simulator.
                    type: boolean
                type: object
              virtualMachineRef:
                description: Reference to the VM to manage.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
            type: object
          status:
            description: VirtualMachineBMCStatus defines the observed state of VirtualMachineBMC.
            properties:
              bootOverride:
                description: |-
                  BootOverride is the currently active boot override, nil when the system
                  boots per its normal boot order.
                properties:
                  bootOrders:
                    additionalProperties:
                      type: integer
                    description: |-
                      BootOrders captures every bootable device present when the oneshot was
                      issued, keyed "<class>:<name>" where class is "disk", "cdrom" or
                      "interface" (matching the BMC device vocabulary) and name is the
                      KubeVirt device name (devices.disks[].name / devices.interfaces[].name).
                      A zero value means the device existed but had no bootOrder (bootOrder
                      counts from 1), which distinguishes it from devices added after the
                      backup was taken.
                    type: object
                  mode:
                    description: BootOverrideMode is the persistence mode of a boot
                      override.
                    enum:
                    - Oneshot
                    - Persistent
                    type: string
                  originalFirmware:
                    description: |-
                      OriginalFirmware records the VM firmware bootloader type when the
                      oneshot backup was taken.
                    enum:
                    - Legacy
                    - UEFI
                    type: string
                  vmiUID:
                    description: |-
                      VMIUID is the UID of the VMI generation current when a oneshot override
                      was issued. A UID change means the oneshot boot was consumed.
                    type: string
                required:
                - mode
                type: object
              clusterIP:
                description: IP address exposed by the BMC service
                type: string
              conditions:
                description: List of current conditions (e.g., Ready)
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
