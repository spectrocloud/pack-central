
# Copyright (c) 2025, NVIDIA CORPORATION.  All rights reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

replicaCount: 1

# Image configuration
image:
  repository: ghcr.io/nvidia/nvsentinel/janitor
  pullPolicy: IfNotPresent
  tag: ""

nameOverride: ""
fullnameOverride: "janitor"

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Automatically mount a ServiceAccount's API credentials?
  automount: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}
podLabels: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000


resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

livenessProbe:
  httpGet:
    path: /
    port: http
readinessProbe:
  httpGet:
    path: /
    port: http

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}

# Extra environment variables to add to the janitor container
# Example:
# extraEnv:
#   - name: MY_VAR
#     value: "my-value"
#   - name: SECRET_VAR
#     valueFrom:
#       secretKeyRef:
#         name: my-secret
#         key: secret-key
extraEnv: []

config:
  # Global timeout - used as default for controllers that don't specify their own timeout
  # Should be set to a reasonable default that works for most operations
  timeout: "25m"
  # host where the provider is running
  cspProviderHost: "janitor-provider.nvsentinel.svc.cluster.local:50051"
  # CSP provider gRPC connection settings
  cspProvider:
    tls:
      # Enable TLS for gRPC connection to CSP provider
      enabled: true
      # Name of the Secret containing the CA certificate (key: ca.crt).
      # Defaults to the janitor-provider's cert secret name.
      caSecretName: "janitor-provider-grpc-cert"
      # Skip TLS verification (for local development only)
      insecure: false
    auth:
      # Enable SA token authentication for gRPC connection to CSP provider
      enabled: true
      # Audience for the projected service account token.
      # The provider validates this audience in TokenReview.
      audience: "nvsentinel-csp-provider"
      # Token expiration in seconds (default: 1 hour, K8s auto-rotates before expiry)
      expirationSeconds: 3600
  # Manual mode - if true, controllers won't send actual reboot/terminate signals
  manualMode: false
  # HTTP endpoint port for exposing runtime configuration
  httpPort: 8082
  # Node exclusions - nodes matching these label selectors will be excluded from janitor operations
  nodes:
    exclusions: []
    # Example:
    # - matchLabels:
    #     node-role.kubernetes.io/control-plane: ""
    # - matchExpressions:
    #   - key: app
    #     operator: In
    #     values:
    #       - critical
  
  # Controller-specific configuration
  controllers:
    # Reboot node controller configuration
    rebootNode:
      # Enable/disable the reboot node controller (default: true)
      enabled: true
      # Timeout for reboot operations (includes CSP API call + node recovery time)
      # If not set or set to empty, defaults to config.timeout (25m)
      # AWS/Azure/OCI: Typically 25m (includes 5min cooldown period)
      # GCP: Can be shorter as operation status is tracked directly
      # kind/kwok: Can be much shorter for testing (5m)
      timeout: "25m"
    
    # Terminate node controller configuration
    terminateNode:
      # Enable/disable the terminate node controller (default: true)
      enabled: true
      # Timeout for terminate operations
      # If not set or set to empty, defaults to config.timeout (25m)
      timeout: "25m"

    # GPU reset controller configuration
    gpuReset:
      # Enable/disable the GPU reset controller (default: true)
      enabled: true
      timeout: "25m"
      serviceManager:
        name: "gpu-operator"
      resetJob:
        runtimeClassName: "nvidia"
        # imagePullSecrets can be specified under image.imagePullSecrets, otherwise we will
        # fall back to global.imagePullSecrets.
        image:
          repository: ghcr.io/nvidia/nvsentinel/gpu-reset
          tag: ""
        resources:
          requests:
            cpu: "50m"
            memory: "64Mi"
          limits:
            cpu: "100m"
            memory: "128Mi"

# Webhook Configuration
webhook:
  # Port for the webhook server
  port: 9443
  # Directory containing webhook TLS certs
  certDir: "/tmp/k8s-webhook-server/serving-certs"
  # Cert-manager issuer to use for webhook certificate
  # Janitor creates its own self-signed issuer
  certIssuer: "janitor-selfsigned-issuer"

# Metrics Configuration
metrics:
  # TLS configuration for metrics server
  tls:
    # Enable TLS for metrics endpoint
    enabled: false
    # Directory containing metrics server TLS certs
    certDir: "/tmp/k8s-metrics-server/metrics-certs"
    # Cert-manager issuer name (defaults to webhook.certIssuer if not set)
    issuerName: ""
    # Cert-manager issuer kind (Issuer or ClusterIssuer)
    issuerKind: "Issuer"
    # Cert-manager issuer group (optional)
    issuerGroup: ""
    # Certificate duration (default: 90 days)
    duration: "2160h"
    # Certificate renewal time before expiry (default: 30 days)
    renewBefore: "720h"
    # Organization name for certificate subject
    organization: "NVIDIA"
