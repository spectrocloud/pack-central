# Copyright (c) 2025, NVIDIA CORPORATION.  All rights reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "janitor.fullname" . }}
  labels:
    {{- include "janitor.labels" . | nindent 4}}
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "janitor.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        # Force pod restart when configmap changes
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        {{- with ((.Values.global).podAnnotations | default .Values.podAnnotations) }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "janitor.selectorLabels" . | nindent 8 }}
    spec:
      {{- with ((.Values.global).imagePullSecrets | default .Values.imagePullSecrets) }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "janitor.serviceAccountName" . }}
      {{- if .Values.global.auditLogging.enabled }}
      securityContext:
        fsGroup: 65532
      {{- end }}
      {{- if .Values.global.auditLogging.enabled }}
      initContainers:
        {{- include "nvsentinel.auditLogging.initContainer" . | nindent 8 }}
      {{- end }}
      containers:
        - name: janitor
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default ((.Values.global).image).tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            - "--metrics-bind-address=:{{ ((.Values.global).metricsPort) | default 2112 }}"
            - "--health-probe-bind-address=:8081"
            - "--config=/etc/nvsentinel/janitor/config.yaml"
            - "--webhook-cert-path={{ .Values.webhook.certDir }}"
            - "--webhook-cert-name=tls.crt"
            - "--webhook-cert-key=tls.key"
            {{- if .Values.metrics.tls.enabled }}
            - "--metrics-secure=true"
            - "--metrics-cert-path={{ .Values.metrics.tls.certDir }}"
            - "--metrics-cert-name=tls.crt"
            - "--metrics-cert-key=tls.key"
            {{- end }}
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            {{- if .Values.global.auditLogging.enabled }}
            {{- include "nvsentinel.auditLogging.envVars" . | nindent 12 }}
            {{- end }}
            {{- with .Values.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          ports:
            - name: metrics
              containerPort: {{ ((.Values.global).metricsPort) | default 2112 }}
            - name: webhook
              containerPort: {{ .Values.webhook.port }}
              protocol: TCP
          volumeMounts:
            - name: config
              mountPath: /etc/nvsentinel/janitor/config.yaml
              subPath: config.yaml
              readOnly: true
            - name: webhook-certs
              mountPath: {{ .Values.webhook.certDir }}
              readOnly: true
            {{- if .Values.metrics.tls.enabled }}
            - name: metrics-certs
              mountPath: {{ .Values.metrics.tls.certDir }}
              readOnly: true
            {{- end }}
            {{- if and .Values.config.cspProvider .Values.config.cspProvider.tls .Values.config.cspProvider.tls.enabled }}
            - name: csp-ca
              mountPath: /etc/nvsentinel/janitor/csp-ca
              readOnly: true
            {{- end }}
            {{- if and .Values.config.cspProvider .Values.config.cspProvider.auth .Values.config.cspProvider.auth.enabled }}
            - name: csp-provider-token
              mountPath: /var/run/secrets/nvsentinel/csp-provider
              readOnly: true
            {{- end }}
            {{- if .Values.global.auditLogging.enabled }}
            {{- include "nvsentinel.auditLogging.volumeMount" . | nindent 12 }}
            {{- end }}
      restartPolicy: Always
      volumes:
        - name: config
          configMap:
            name: {{ include "janitor.fullname" . }}
        - name: webhook-certs
          secret:
            secretName: {{ include "janitor.fullname" . }}-webhook-cert
            defaultMode: 420
        {{- if .Values.metrics.tls.enabled }}
        - name: metrics-certs
          secret:
            secretName: {{ include "janitor.fullname" . }}-metrics-cert
            items:
              - key: ca.crt
                path: ca.crt
              - key: tls.crt
                path: tls.crt
              - key: tls.key
                path: tls.key
            defaultMode: 420
        {{- end }}
        {{- if and .Values.config.cspProvider .Values.config.cspProvider.tls .Values.config.cspProvider.tls.enabled }}
        - name: csp-ca
          secret:
            secretName: {{ .Values.config.cspProvider.tls.caSecretName }}
            items:
              - key: ca.crt
                path: ca.crt
            defaultMode: 420
        {{- end }}
        {{- if and .Values.config.cspProvider .Values.config.cspProvider.auth .Values.config.cspProvider.auth.enabled }}
        - name: csp-provider-token
          projected:
            sources:
              - serviceAccountToken:
                  audience: {{ .Values.config.cspProvider.auth.audience | quote }}
                  expirationSeconds: {{ .Values.config.cspProvider.auth.expirationSeconds | default 3600 }}
                  path: token
        {{- end }}
        {{- if .Values.global.auditLogging.enabled }}
        {{- include "nvsentinel.auditLogging.volume" . | nindent 8 }}
        {{- end }}
      {{- with (((.Values.global).systemNodeSelector) | default .Values.nodeSelector) }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with (((.Values.global).affinity) | default .Values.affinity) }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with (((.Values.global).systemNodeTolerations) | default .Values.tolerations) }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
